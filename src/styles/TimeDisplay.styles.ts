import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    justifyContent: 'center',
  },
  time: {
    fontSize: 72,
    fontWeight: 'bold',
    textAlign: 'center',
  },
  prefix: {
    fontSize: 72,
    fontWeight: 'bold',
    textAlign: 'right',
    minWidth: 40,
  },
}); 